#include<stdio.h>

int main(void)
{
    int ary[];
    int i=0, n;
    while(i<=99)
    {
        scanf("%d",&arr[i]);

        if(arr[i]=q )
        {
            continue;
            }
        i++;
        }
    while(i<=99)
    {
        for(n=0; n<100; n++)
        {
            if
            }
        }
    }
